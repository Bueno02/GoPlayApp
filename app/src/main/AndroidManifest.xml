<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:dist="http://schemas.android.com/apk/distribution"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.e.goplay">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <dist:module dist:instant="true"/>

    <application
            android:usesCleartextTraffic="true"
            android:allowBackup="true"
            android:icon="@mipmap/ic_logo_green_foreground"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_logo_green_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".CadastroActivity">
        </activity>
        <activity android:name=".UserInfo">
        </activity>
        <activity android:name=".EntraGameActivity">
        </activity>
        <activity android:name=".NewGame">
        </activity>
        <activity
                android:name=".GamesActivity"
                android:label="@string/title_activity_games">
        </activity>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>